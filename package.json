{
  "name": "dailybite",
  "version": "0.1.0",
  "private": true,
  "description": "Static food-deal intelligence app with a review-first publishing pipeline.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node pipeline/run.mjs --validate-only",
    "publish-data": "node pipeline/run.mjs",
    "scan": "node pipeline/scan.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
